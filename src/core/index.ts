import useNavigation from 'navigator/Navigator';
import { types } from 'common/enums/types';
import { keyCodeKeyBoard, keyCodeWheel } from 'common/enums/key-codes';

export {
  types,
  keyCodeKeyBoard,
  keyCodeWheel,
};

export default useNavigation;
